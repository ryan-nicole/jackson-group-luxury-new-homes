function money(v){return new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(v||0)}document.querySelectorAll('[data-calculator]').forEach(c=>{const p=c.querySelector('[data-price]'),m=c.querySelector('[data-commission]'),s=c.querySelector('[data-share]'),r=c.querySelector('[data-rebate-result]'),g=c.querySelector('[data-gross-result]');function u(){const gross=Number(p.value||0)*(Number(m.value||0)/100),reb=gross*(Number(s.value||0)/100);r.textContent=money(reb);g.textContent='Based on estimated compensation of '+money(gross)}[p,m,s].forEach(i=>i.addEventListener('input',u));u()});const b=document.getElementById('journeyContinue'),msg=document.getElementById('journeyMessage');if(b){b.addEventListener('click',()=>{const x=[...document.querySelectorAll('.journey-choice input:checked')].map(i=>i.value);if(!x.length){msg.textContent='Choose at least one option so we can guide you.';return}if(x.includes('new-construction')&&x.includes('rebates')){location.href='register/index.html';return}if(x.includes('sell-current')){location.href='sell/index.html';return}if(x.includes('rebates')){location.href='rebates/index.html';return}if(x.includes('new-construction')){location.href='communities/index.html';return}msg.textContent='Great — we’ll build a first-time buyer pathway into the next sprint.'})}