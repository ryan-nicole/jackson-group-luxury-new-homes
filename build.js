const fs=require('fs');const cp=require('child_process');fs.rmSync('dist',{recursive:true,force:true});fs.mkdirSync('dist',{recursive:true});fs.cpSync('assets','dist/assets',{recursive:true});const b64=fs.readFileSync('.source/site-text.tar.gz.b64','utf8').replace(/\s+/g,'');fs.writeFileSync('/tmp/jg-site.tar.gz',Buffer.from(b64,'base64'));cp.execFileSync('tar',['-xzf','/tmp/jg-site.tar.gz','-C','dist']);console.log('Jackson Group clean master built successfully.');